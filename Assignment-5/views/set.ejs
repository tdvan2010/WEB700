<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>Set Details</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/theme.css">
</head>
<body>
  <%- include('partials/navbar', { page: '' }) %>

  <header class="py-5 text-center">
    <h1 class="display-5"><%= set.name %></h1>
    <p class="lead">Below, you will find detailed information about the set: <strong><%= set.set_num %></strong></p>
  </header>

  <main class="container pb-5">
    <div class="row g-4 align-items-center">
      <div class="col-md-5">
        <img class="img-fluid rounded shadow" src="<%= set.img_url %>" alt="Image of <%= set.name %>">
      </div>
      <div class="col-md-7">
        <ul class="list-group list-group-flush">
          <li class="list-group-item bg-transparent text-light"><strong>Name:</strong> <%= set.name %></li>
          <li class="list-group-item bg-transparent text-light"><strong>Year:</strong> <%= set.year %></li>
          <li class="list-group-item bg-transparent text-light"><strong>Theme:</strong> <%= set.theme || ('#' + set.theme_id) %></li>
          <li class="list-group-item bg-transparent text-light"><strong>Parts:</strong> <%= set.num_parts %></li>
        </ul>
        <div class="mt-4 d-flex gap-2">
          <a class="btn btn-danger" href="/lego/deleteSet/<%= set.set_num %>">Delete</a>
          <a class="btn btn-outline-light" href="#" onclick="history.back(); return false;">Back</a>
        </div>
      </div>
    </div>
  </main>
</body>
</html>